import { createStore } from "vuex";
import NavMenu from "@/store/NavMenu";
export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    navMenu: NavMenu,
  },
});
